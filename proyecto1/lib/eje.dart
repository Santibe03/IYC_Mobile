import 'package:flutter/material.dart';

class adso extends StatelessWidget {
  const adso({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueGrey,
      child: const Center(child: Text('Hello, Flutter!'))
    );
  }
}